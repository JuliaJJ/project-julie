---
import { Icon } from 'astro-icon';

import { getCollection } from "astro:content";
const allFeedPosts = await getCollection('feed');
console.log(allFeedPosts);

---

{allFeedPosts.map(feedEntry => (
   <div class="col-auto">
      <div class="flex bg-slate-100 shadow-lg rounded-lg mx-4 md:mx-auto max-w-md md:max-w-2xl relative hover:bg-white">
         <a href="#" class="absolute w-5 -left-2 -top-2"><Icon name="circle-plus-solid" class="hover:fill-sky-900" /></a>
         <a href="#" class="inline-block ml-4 mb-2 absolute top-5 right-5 w-6"><Icon name={`${feedEntry.data.mediaType}`} class="hover:fill-sky-900" /></a>
         <div class="flex flex-col items-start p-5 w-full">
            <a href={`${feedEntry.data.url}`}><img class="w-16 object-cover mb-4" src={`${feedEntry.data.thumbnail}`} alt="media"></a>
            <div class="w-full">
               <div class="items-center justify-between">
                  <a href={`${feedEntry.data.url}`}><h2 class="text-sm font-semibold text-gray-900 -mt-1">{feedEntry.data.title}</h2></a>      
               </div>
               <p class="text-sm text-gray-700">{feedEntry.data.subtitle}</p>
               <small class="text-xs text-gray-700 float-right">{feedEntry.data.pubDate.toDateString()}</small>
            </div>
         </div>
      </div>
   </div>
))}